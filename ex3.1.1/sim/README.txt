In order to simulate the register file using our script follow these steps:
1. Launch questaSim
2. Within the QuestaSim's console run "source simulation_registerfile.scr" to compile and simulate the register file
